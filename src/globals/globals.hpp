#ifndef __GLOBALS_HPP
#define __GLOBALS_HPP

// Useful for suppressing "unused parameter" warnings on purpose for some things, for example main's argc/argv or some
// constants that are not used but are still there to have all possible things cleanly designed.
#define UNUSED(x) (void)(x)

#endif /* __GLOBALS_HPP */
